// STUB: leaf image upload widget for the disease detection page.
// TODO: build the drag & drop + preview UI, then call detectDisease()
// from src/api/disease.js once the Flask endpoint exists.
export default function LeafImageUpload() {
  return null;
}
